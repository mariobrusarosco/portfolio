# Server-only OpenAI key used by src/routes/api/chat.ts.
# Do not prefix this with VITE_, PUBLIC_, or NEXT_PUBLIC_.
OPENAI_API_KEY=

# Optional. Defaults to gpt-5-mini when omitted.
OPENAI_MODEL=gpt-5-mini
